# Task TODO

- [x] Inspect existing `home.blade.php` and identify missing sidebar layout.
- [x] Implement dashboard layout (sidebar + main content) in `resources/views/home.blade.php`.
- [x] Ensure white background on app shell (wrapper + sidebar + main).
- [x] Preserve existing profile fetch (`/api/me`) and logout functionality.
- [x] Quick manual test steps for `/home`.

- [ ] Fix login page centering robustness and ensure card never disappears (`resources/views/login.blade.php`).
- [ ] Add/ensure profile icon area in login card if missing (`resources/views/login.blade.php`).
- [ ] Make home page sidebar stay exactly full viewport height (`h-screen`) (`resources/views/home.blade.php`).
- [ ] Clean up home page header layout so it aligns with main content container (`resources/views/home.blade.php`).
- [ ] Add profile/avatar icon to home header dropdown trigger (`resources/views/home.blade.php`).
- [ ] Manual test `/login` and `/home` after changes.

